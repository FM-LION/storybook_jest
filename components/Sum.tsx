function sum(x: number, y: number): number {
    return x + y;
}

function minus(x: number, y: number): number {
    return x - y;
}

function mutiple(x: number, y: number): number {
    return x * y;
}

 export default sum;

 export {minus, mutiple}